method: grid

metric:
  name: best_macro_f1
  goal: maximize

parameters:
  max_len:
    values: [32, 64, 128]

  embed_dim:
    values: [50, 100]

  learning_rate:
    values: [1e-3, 5e-4]

  dropout:
    value: [0.3, 0.5, 0.6]

  hidden_dim:
    value: [32, 64, 128]
